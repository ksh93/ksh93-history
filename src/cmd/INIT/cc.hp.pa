: hp.pa cc wrapper for reasonable ansi C defaults : 2003-06-08 :

HOSTTYPE=hp.pa

case $* in
-dumpmachine) echo $HOSTTYPE; exit ;;
esac

/opt/ansic/bin/cc -Ae +e -Wl,+s \
	${INSTALLROOT:+-Wl,+cdp,${INSTALLROOT}/lib/:} \
	-Wl,+vnocompatwarnings "$@"
